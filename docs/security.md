# 安全边界

- 配对码为六位随机数字，默认 300 秒过期且单次使用。
- 服务端只保存 SHA-256 设备凭证摘要，不保存可重放的原始凭证。
- 设备可由 AstrBot WebUI 撤销；撤销后凭证立即失效。
- 该插件不自动开放公网。使用外网时，部署者负责 HTTPS、反向代理或隧道配置。
- 日志、文档、示例和异常响应不得输出 Credential、Authorization、Cookie 或用户消息以外的密钥。
- 当前阶段不注册拍照、系统控制、文件访问或其他高风险 Tool。
